version: '3'

services:
  db:
    image: postgres
    volumes:
      - /var/lib/postgresql/data
  web:
    build: .
    command: bash startup.sh
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db